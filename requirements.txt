# Flask backend
Flask>=2.3
Flask-SQLAlchemy>=3.1
requests>=2.31

# For extracting browser cookies (Chrome, Firefox)
browser-cookie3>=0.19

# Dependencies for browser-cookie3 (cross-platform)
keyring>=24.0
cryptography>=41.0
pycryptodome>=3.20

# Optional: better system info or cross-platform enhancements
psutil>=5.9
colorama>=0.4
